spring:
  application:
    name: restaurant
  datasource:
    url: jdbc:h2:file:./data/RestauranteDb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: rafaeldev
    password: vitordev
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console